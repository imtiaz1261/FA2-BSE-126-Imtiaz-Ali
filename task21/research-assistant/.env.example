# Copy this file to ".env" and fill in your own Groq key.
# NEVER commit a real key to source control -- .env is gitignored.

# --------------------------------------------------------------------
# Groq (required) -- free key, no credit card: https://console.groq.com/keys
# --------------------------------------------------------------------
GROQ_API_KEY=grokapikey
GROQ_MODEL=llama-3.3-70b-versatile
LLM_TEMPERATURE=0.1

# --------------------------------------------------------------------
# Web search (DuckDuckGo -- no API key needed)
# --------------------------------------------------------------------
SEARCH_RESULTS_PER_TASK=5

# --------------------------------------------------------------------
# Research workflow
# --------------------------------------------------------------------
MAX_TASKS=5
MAX_RETRIES_PER_TASK=2
MIN_RESULTS_FOR_SUFFICIENCY=2

# --------------------------------------------------------------------
# Paths
# --------------------------------------------------------------------
OUTPUTS_DIR=./outputs
LOG_DIR=./logs

# --------------------------------------------------------------------
# FastAPI / Streamlit
# --------------------------------------------------------------------
API_HOST=127.0.0.1
API_PORT=8000
