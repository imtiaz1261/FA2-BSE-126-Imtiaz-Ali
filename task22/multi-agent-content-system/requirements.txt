# Target Python: 3.10-3.12 (see README for why 3.14 is not recommended
# for this stack — same tokenizers/wheel-availability issues as your
# earlier RAG and fine-tuning projects).

# Multi-agent orchestration
langgraph>=0.2.0,<0.3.0
langchain>=0.3.0,<0.4.0
langchain-core>=0.3.0,<0.4.0

# LLM provider (Groq — free, OpenAI-compatible API)
openai>=1.30.0

# Web search tool (free, no API key required)
duckduckgo-search>=6.0.0

# Configuration & data validation
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0

# CLI
typer>=0.12.0
rich>=13.7.0

# Document export
python-docx>=1.1.0
reportlab>=4.1.0
markdown>=3.6.0

# Web API (added when we build the FastAPI layer)
fastapi>=0.110.0
uvicorn>=0.29.0

# Testing
pytest>=8.0.0
