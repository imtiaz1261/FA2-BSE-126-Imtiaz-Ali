# Copy to .env and fill in real values locally. Never commit a filled-in
# .env file. DATABASE_URL should point at the same shared Postgres instance
# used by the other services.

PORT=8500
DATABASE_URL=postgresql://tracking_service_user:REPLACE_WITH_YOUR_DB_PASSWORD@localhost:5432/appointments
