openai>=1.30.0
sounddevice>=0.4.6
numpy>=1.24.0
edge-tts>=6.1.0

# No audio-playback package needed — playback uses Windows' built-in
# winmm.dll via Python's standard library ctypes module, so there's
# nothing here that can fail to compile.