scikit-learn>=1.3
numpy>=1.24
flask>=2.3

# Optional — only needed if you use --backend embeddings
# (better semantic quality, needs internet on first run to download the model)
# sentence-transformers>=2.2
