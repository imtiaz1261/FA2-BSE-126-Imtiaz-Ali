# Copy this file to ".env" and fill in your own values.
# Only GROQ_API_KEY is required -- everything else has a free/local default.

# --------------------------------------------------------------------
# Groq (required) -- free key, no credit card: https://console.groq.com/keys
# NEVER commit a real key to source control; this file is a template only.
# --------------------------------------------------------------------
GROQ_API_KEY=your-groq-api-key-here
GROQ_MODEL=llama-3.1-8b-instant
LLM_TEMPERATURE=0.2

# --------------------------------------------------------------------
# Paths
# --------------------------------------------------------------------
DATA_DIR=./data
VECTOR_DB_DIR=./vector_store
DB_PATH=./db/assistant.db
LOG_DIR=./logs

# --------------------------------------------------------------------
# Web search (DuckDuckGo -- no API key needed)
# --------------------------------------------------------------------
SEARCH_MAX_RESULTS=5

# --------------------------------------------------------------------
# Document RAG (file reading tool)
# --------------------------------------------------------------------
CHUNK_SIZE=1000
CHUNK_OVERLAP=200
EMBEDDING_MODEL=all-MiniLM-L6-v2
RAG_TOP_K=4

# --------------------------------------------------------------------
# Conversation memory
# --------------------------------------------------------------------
MAX_MEMORY_TURNS=20

# --------------------------------------------------------------------
# Voice (text-to-speech)
# --------------------------------------------------------------------
TTS_RATE=175
TTS_VOLUME=1.0
