# Single requirements file for the whole project (backend + frontend).
# Install once at the project root:  pip install -r requirements.txt

# ---- Backend: FastAPI core (Phase 1-2) ----
fastapi>=0.115.0
uvicorn[standard]>=0.30.6
pydantic>=2.9.2
pydantic-settings>=2.5.2
python-dotenv>=1.0.1
httpx>=0.27.2

# ---- Backend: Database (Phase 3) ----
sqlalchemy>=2.0.35
alembic>=1.13.2
psycopg[binary]>=3.2.1
pgvector>=0.3.4

# ---- Frontend: Streamlit core (Phase 1-2) ----
streamlit>=1.38.0
requests>=2.32.3

# ---- Phase 4: Auth ----
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0
email-validator>=2.2.0

# ---- Phase 6-7: LLM chat + streaming ----
openai>=1.47.0

# ---- Phase 8: Document upload ----
python-multipart>=0.0.9

# ---- Phase 9-13: RAG / Agents / Tools ----
rank-bm25>=0.2.2
sentence-transformers>=3.1.1

# ---- Phase 11-12: LangGraph Agent + Tools ----
langgraph>=0.2.28
langchain-core>=0.3.0
tavily-python>=0.5.0

# ---- Phase 9: Document text extraction ----
pypdf>=4.3.1
python-docx>=1.1.2

# ---- Phase 13: Deep Research export ----
markdown>=3.6

# ---- Phase 14: Guardrails ----
# (no new deps — uses openai + regex)

# ---- Phase 15: SaaS / Payments ----
stripe>=10.10.0
redis>=5.0.8
slowapi>=0.1.9

# ---- Phase 17: Observability ----
langfuse>=2.48.0

# ---- Phase 18: Admin dashboard charts ----
plotly>=5.24.0
pandas>=2.2.2

# ---- Pinned for Python 3.14 compat ----
passlib[bcrypt]==1.7.4
bcrypt==4.2.1
